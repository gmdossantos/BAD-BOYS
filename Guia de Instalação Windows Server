Guia simplificado para instalação e configuração do Windows Server 2022 como Controlador de Domínio.
📋 Requisitos Mínimos

Processador: 1,4 GHz 64 bits
RAM: 4 GB (16 GB recomendado para produção)
Armazenamento: 60 GB (500 GB SSD para produção)
Rede: Adaptador Gigabit Ethernet

🚀 Instalação Rápida
1. Configuração Inicial
powershell# Configurar IP estático
New-NetIPAddress -IPAddress 192.168.10.10 -PrefixLength 24 -DefaultGateway 192.168.10.1

# Renomear servidor
Rename-Computer -NewName "DC01" -RestartIfRequired
2. Instalar Funções Necessárias
powershell# Instalar Active Directory e DNS
Install-WindowsFeature -Name AD-Domain-Services -IncludeManagementTools
Install-WindowsFeature -Name DNS -IncludeManagementTools
Install-WindowsFeature -Name DHCP -IncludeManagementTools
Install-WindowsFeature -Name GPMC -IncludeManagementTools
3. Configurações de Segurança
powershell# Habilitar auditoria
auditpol /set /category:"Logon/Logoff" /success:enable /failure:enable
auditpol /set /category:"Account Management" /success:enable /failure:enable

# Configurar firewall para AD
New-NetFirewallRule -DisplayName "Active Directory-LDAP" -Direction Inbound -Protocol TCP -LocalPort 389 -Action Allow
New-NetFirewallRule -DisplayName "DNS" -Direction Inbound -Protocol TCP -LocalPort 53 -Action Allow
New-NetFirewallRule -DisplayName "Kerberos" -Direction Inbound -Protocol TCP -LocalPort 88 -Action Allow
4. Configurar NTP
powershell# Sincronizar horário
w32tm /config /manualpeerlist:"pool.ntp.org,0x8" /syncfromflags:MANUAL
w32tm /config /reliable:yes
Restart-Service w32time
🔧 Scripts Úteis
Monitoramento do Servidor
Script salvo em C:\Scripts\Monitor-Server.ps1:
powershell# Verificar serviços críticos
$criticalServices = @("DNS", "DHCP", "Netlogon", "ADWS", "KDC")
foreach ($service in $criticalServices) {
    $svc = Get-Service -Name "*$service*" -ErrorAction SilentlyContinue | Select-Object -First 1
    if ($svc) {
        Write-Host "$($svc.Name): $($svc.Status)"
    }
}
Verificação de Sistema
powershell# Testar conectividade
Test-NetConnection -ComputerName "google.com" -Port 80

# Verificar configuração de rede
Get-NetIPConfiguration

# Status do sistema
Get-ComputerInfo | Select-Object WindowsProductName, WindowsVersion
✅ Checklist de Instalação

 Windows Server 2022 instalado
 Servidor renomeado (DC01)
 IP estático configurado
 Active Directory Domain Services instalado
 DNS configurado
 Firewall configurado
 Auditoria habilitada
 NTP sincronizado
 Scripts de monitoramento criados

🔄 Próximos Passos

Promoção a DC: Configurar domínio e floresta
DNS Avançado: Configurar zonas internas
DHCP: Definir escopos e opções
Group Policy: Criar GPOs de segurança
Backup: Implementar estratégia de backup

📚 Recursos

Microsoft Docs
PowerShell Gallery
TechNet

⚠️ Notas Importantes

Sempre testar em ambiente de laboratório primeiro
Fazer backup antes de mudanças críticas
Manter documentação atualizada
Monitorar logs regularmente


Versão: Windows Server 2022 Standard
Última atualização: Maio 2025
